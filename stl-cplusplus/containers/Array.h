#ifndef ARRAY_H
#define ARRAY_H

// std::array — fixed-size sequence container (size known at compile time)
// Lives on the stack, no heap allocation, no overhead.
// Header: <array>

class Array
{
public:
    Array();
    static void demo();
};

#endif // ARRAY_H
