#ifndef QUEUE_H
#define QUEUE_H

// std::queue — container adaptor implementing FIFO (First In, First Out)
// Wraps another container (default: std::deque) and restricts its interface.
// Header: <queue>

class Queue
{
public:
    Queue();
    static void demo();
};

#endif // QUEUE_H
