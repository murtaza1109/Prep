#ifndef LIST_H
#define LIST_H

// std::list — doubly linked list
// O(1) insert/erase anywhere (given an iterator). No random access.
// Header: <list>

class List
{
public:
    List();
    static void demo();
};

#endif // LIST_H
