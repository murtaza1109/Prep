#ifndef SET_H
#define SET_H

// std::set — ordered collection of UNIQUE elements
// Sorted automatically. Backed by a Red-Black Tree, like std::map.
// All operations (insert, find, erase) are O(log n).
// Header: <set>

class Set
{
public:
    Set();
    static void demo();
};

#endif // SET_H
