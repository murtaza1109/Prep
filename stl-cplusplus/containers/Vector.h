#ifndef VECTOR_H
#define VECTOR_H

// std::vector — dynamic array (the most commonly used STL container)
// Grows automatically on the heap. Elements are stored contiguously.
// Header: <vector>

class Vector
{
public:
    Vector();
    static void demo();
};

#endif // VECTOR_H
