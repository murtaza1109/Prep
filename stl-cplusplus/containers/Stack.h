#ifndef STACK_H
#define STACK_H

// std::stack — container adaptor implementing LIFO (Last In, First Out)
// Wraps another container (default: std::deque) and restricts its interface.
// Header: <stack>

class Stack
{
public:
    Stack();
    static void demo();
};

#endif // STACK_H
