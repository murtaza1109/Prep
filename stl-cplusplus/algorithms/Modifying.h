#ifndef MODIFYING_H
#define MODIFYING_H

// Modifying sequence algorithms from <algorithm>
// Covers: copy/copy_if, transform, fill/fill_n,
//         replace/replace_if, remove/remove_if (erase-remove idiom),
//         unique, reverse, rotate, shuffle

class Modifying
{
public:
    Modifying();
    static void demo();
};

#endif // MODIFYING_H
