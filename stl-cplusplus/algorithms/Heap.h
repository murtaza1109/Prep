#ifndef HEAP_H
#define HEAP_H

// Heap algorithms from <algorithm>
// Covers: make_heap, push_heap, pop_heap, sort_heap, is_heap, is_heap_until

class Heap
{
public:
    Heap();
    static void demo();
};

#endif // HEAP_H
