#ifndef SETOPS_H
#define SETOPS_H

// Set / merge algorithms from <algorithm> (operate on sorted ranges)
// Covers: merge, inplace_merge, set_union, set_intersection,
//         set_difference, set_symmetric_difference, includes

class SetOps
{
public:
    SetOps();
    static void demo();
};

#endif // SETOPS_H
